<Window x:Class="EMGFeedbackSystem.Views.SubjectSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="搜索受试者" 
        Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#FFF5F5F5">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="搜索受试者" FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,10" HorizontalAlignment="Center" Foreground="#FF4A90D9"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchTextBox" Width="400" Margin="0,0,10,0" Padding="5" FontSize="14"/>
            <Button Content="搜索" Click="SearchButton_Click" Width="80" Padding="10,5" 
                    Background="#FF4A90D9" Foreground="White" BorderThickness="0"/>
            <Button Content="显示全部" Click="ShowAllButton_Click" Width="80" Padding="10,5" 
                    Margin="10,0,0,0" Background="#FF6B8E9F" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="SubjectsDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" Background="White"
                  GridLinesVisibility="Horizontal" BorderBrush="#FF4A90D9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="编号" Binding="{Binding SubjectId}" Width="100"/>
                <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="100"/>
                <DataGridTextColumn Header="性别" Binding="{Binding Gender}" Width="60"/>
                <DataGridTextColumn Header="年龄" Binding="{Binding Age}" Width="60"/>
                <DataGridTextColumn Header="上限基准" Binding="{Binding UpperLimit, StringFormat=F2}" Width="80"/>
                <DataGridTextColumn Header="左腿A最大值" Binding="{Binding LeftLegMaxA, StringFormat=F3}" Width="100"/>
                <DataGridTextColumn Header="右腿A最大值" Binding="{Binding RightLegMaxA, StringFormat=F3}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="选择" Click="SelectButton_Click" Width="100" Padding="10,5" 
                    Margin="5" Background="#FF4A90D9" Foreground="White" BorderThickness="0"/>
            <Button Content="取消" Click="CancelButton_Click" Width="100" Padding="10,5" 
                    Margin="5" Background="#FF999999" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
