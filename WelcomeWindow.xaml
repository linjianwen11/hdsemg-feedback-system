<Window x:Class="EMGFeedbackSystem.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="欢迎"
        Width="900"
        Height="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F7FA">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Margin="40" CornerRadius="16" Background="White" BorderBrush="#E3E8EF" BorderThickness="1">
            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="欢迎使用智能远程康复设备"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="#1F2937"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,10"/>

                <TextBlock Grid.Row="1"
                           Text="请点击测试进入评估界面"
                           FontSize="16"
                           Foreground="#6B7280"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <Border Grid.Row="2" Background="#EEF2FF" CornerRadius="12" Padding="24" Margin="0,10,0,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="准备开始"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="#1F2937"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,12"/>
                        <TextBlock Text="点击下方按钮进入测试界面"
                                   FontSize="14"
                                   Foreground="#6B7280"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
            <Button Content="开始测试"
                    Width="160"
                    Height="46"
                    Margin="0,0,16,0"
                    Background="#2563EB"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="16"
                    Click="StartTest_Click"/>
            <Button Content="离线分析"
                    Width="160"
                    Height="46"
                    Background="#10B981"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="16"
                    Click="StartAnalysis_Click"/>
        </StackPanel>
    </Grid>
</Window>
